# Vercel SPA Redirects for Vue Router History Mode
# This file ensures that all client-side routes are properly handled

# API routes should not be redirected
/api/* /api/:splat 200

# All other routes should serve the index.html for client-side routing
/* /index.html 200
